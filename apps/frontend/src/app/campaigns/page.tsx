import { AppShell } from '@/components/layout/app-shell';

export default function CampaignsPage() {
  return <AppShell title="Bulk Calling Campaigns"><div className="rounded-xl border border-slate-800 bg-slate-900 p-5">CSV upload, CRM import, scheduling, concurrency control, live progress and retries.</div></AppShell>;
}
